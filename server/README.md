# SprintFlow AI — Server

Express + MongoDB starter. Install dependencies and run the dev server.

Commands:

```powershell
cd server
npm install
npm run dev
```

Copy `.env.example` to `.env` and set `MONGO_URI` and `JWT_SECRET`.
